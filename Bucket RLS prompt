You are an expert in Supabase Storage and PostgreSQL Row Level Security (RLS).

### Storage Bucket

Bucket name: `[BUCKET_NAME]`

### Access Rules

`[WHAT USERS SHOULD AND SHOULD NOT BE ABLE TO DO WITH FILES]`

### File Structure & Ownership

`[HOW FILES ARE ORGANIZED AND WHO SHOULD OWN OR HAVE ACCESS TO THEM]`

Based on this information, generate the required Supabase Storage RLS policies for the `storage.objects` table.

Choose the appropriate operation(s): SELECT, INSERT, UPDATE, DELETE, or ALL.

Make sure every policy applies only to `[BUCKET_NAME]`.

Use the appropriate Supabase Storage fields and PostgreSQL RLS expressions to correctly enforce the requested access and ownership rules.

Do not assume a particular ownership mechanism unless it matches the requirements provided above.

Return:

1. The SQL code I can copy and paste into the Supabase SQL Editor.
2. A short explanation of what each policy does.

If important information is missing, ambiguous, or contradictory, ask me before generating the SQL.
